pipeline:
  - step: train_model
    dataset_path: ml/checkpoints/dataset_5k.pt
    architecture: CNN
    epochs: 10
    batch_size: 64
    learning_rate: 0.001
    output_onnx: ml/checkpoints/genesynth_model.onnx
